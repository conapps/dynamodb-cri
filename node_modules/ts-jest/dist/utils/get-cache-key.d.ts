import { JestCacheKeyOptions } from '../types';
export default function getCacheKey(fileData: string, filePath: string, jestConfigString: string, { instrument, rootDir }: JestCacheKeyOptions): string;
